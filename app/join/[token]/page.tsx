import { notFound } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
import JoinTeamForm from "./JoinTeamForm";
export default async function JoinPage({params}:{params:Promise<{token:string}>}){const {token}=await params;if(!/^[a-f0-9]{64}$/.test(token))notFound();const supabase=await createClient();const {data}=await supabase.rpc("get_team_invitation",{invitation_token:token});const invite=data?.[0];if(!invite?.is_valid)return <main className="flex min-h-screen flex-1 items-center justify-center bg-[#f6f8f7] p-6"><div className="max-w-md rounded-2xl bg-white p-8"><h1 className="text-2xl font-bold">Invitation unavailable</h1><p className="mt-3 text-slate-600">This invitation expired, was revoked, or has already been used. Ask the organization owner for a new link.</p></div></main>;return <main className="flex min-h-screen flex-1 items-center justify-center bg-[#f6f8f7] p-6 text-slate-900"><div className="w-full max-w-md rounded-2xl border border-slate-200 bg-white p-8"><p className="text-sm font-bold uppercase tracking-widest text-sky-700">Team invitation</p><h1 className="mt-3 text-3xl font-bold">Join {invite.organization_name}</h1><p className="mt-2 text-slate-600">You were invited as {invite.role.replace("_"," ")}.</p><JoinTeamForm token={token} email={invite.email}/></div></main>}
